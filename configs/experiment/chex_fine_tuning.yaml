# @package _global_

defaults:
  - chex_fine_tuning_base.yaml

# general settings
task_name: chex_fine_tuning

# override model settings
model:
  backbone: resnet50
  ckpt_path: /fsx/aimi/siaug/checkpoints/fine_tuning/lcls_chex_siam_rrc_jit_light-last.pt
