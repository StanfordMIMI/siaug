# @package _global_

defaults:
  - eval_chex_frontal_base.yaml

# general settings
task_name: eval_chex_frontal_resnet

# override dataloader settings
# dataloader:
#   valid:
#     batch_size: 4

# override model settings 
model:
  backbone: resnet50
