# @package _global_

defaults:
  - vindr_fully_supervised_base.yaml

# general settings
task_name: vindr_fully_supervised_pretrained

# override model settings
model:
  backbone: resnet50
  pretrained: True
