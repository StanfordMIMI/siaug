# @package _global_

defaults:
  - lcls_chex_base.yaml

# general settings
task_name: lcls_rebuttal_chex_on_mimic_rrc_jitter

# override model settings
model:
  backbone: resnet50
  ckpt_path: /fsx/aimi/siaug/rebuttal/repr_mimic_siam_rrc_jitter-last.pt
