# @package _global_

defaults:
  - chex_fully_supervised_base.yaml

# general settings
task_name: chex_fully_supervised_pretrained_vit_small_patch16_224_dino

# override model settings
model:
  backbone: vit_small_patch16_224_dino
  pretrained: True
