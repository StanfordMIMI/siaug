# @package _global_

defaults:
  - lcls_chex_base.yaml

# general settings
task_name: lcls_chex_siam_rrc_jit_light

# override model settings
model:
  backbone: resnet50
  ckpt_path: /fsx/aimi/siaug/checkpoints/fine_tuning/repr_mimic_siam_rrc_jit_light-last.pt
