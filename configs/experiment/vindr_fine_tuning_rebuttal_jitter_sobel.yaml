# @package _global_

defaults:
  - vindr_fine_tuning_base.yaml

# general settings
task_name: vindr_fine_tuning_rebuttal_jitter_sobel

# override model settings
model:
  backbone: resnet50
  ckpt_path: /fsx/aimi/siaug/rebuttal/lcls_vindr_jitter_sobel.pt
