# @package _global_

defaults:
  - vindr_fine_tuning_base.yaml

# general settings
task_name: vindr_fine_tuning_rebuttal_rrc_noise

# override model settings
model:
  backbone: resnet50
  ckpt_path: /fsx/aimi/siaug/rebuttal/lcls_vindr_rrc_noise.pt
