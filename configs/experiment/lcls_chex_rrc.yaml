# @package _global_

defaults:
  - lcls_chex_base.yaml

# general settings
task_name: lcls_chex_rrc

# override model settings
model:
  backbone: resnet50
  ckpt_path: ${paths.log_dir}/chexpert_rrc/runs/2022-10-05_14-16-11/checkpoints/epoch-0099.pt/pytorch_model.bin
