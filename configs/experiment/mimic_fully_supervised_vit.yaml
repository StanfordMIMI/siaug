# @package _global_

defaults:
  - mimic_fully_supervised_base.yaml

# general settings
task_name: mimic_fully_supervised_vit_small_patch16_224_dino

# override model settings
model:
  backbone: vit_small_patch16_224_dino