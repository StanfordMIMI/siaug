# @package _global_

defaults:
  - chex_fully_supervised_base.yaml

# general settings
task_name: chex_fully_supervised_pretrained

# override model settings
model:
  backbone: resnet50
  pretrained: True
