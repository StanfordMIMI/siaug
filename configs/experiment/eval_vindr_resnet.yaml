# @package _global_

defaults:
  - eval_vindr_base.yaml

# general settings
task_name: eval_vindr_resnet

# override dataloader settings
# dataloader:
#   valid:
#     batch_size: 4

# override model settings 
model:
  backbone: resnet50
