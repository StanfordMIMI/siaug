# @package _global_

defaults:
  - lcls_chex_base.yaml

# general settings
task_name: lcls_chex_jitter

# override model settings
model:
  backbone: resnet50
  ckpt_path: ${paths.log_dir}/repr_chex_jitter/runs/2022-10-08_13-13-34/checkpoints/epoch-0099.pt/pytorch_model.bin
